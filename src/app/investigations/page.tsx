import { SectionPage, sectionMetadata } from '@/components/pages/SectionPage';

export const metadata = sectionMetadata('investigations');

export default function Page() {
  return <SectionPage id="investigations" />;
}
