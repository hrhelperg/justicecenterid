import { SectionPage, sectionMetadata } from '@/components/pages/SectionPage';

export const metadata = sectionMetadata('law-enforcement');

export default function Page() {
  return <SectionPage id="law-enforcement" />;
}
