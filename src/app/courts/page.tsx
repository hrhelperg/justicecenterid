import { SectionPage, sectionMetadata } from '@/components/pages/SectionPage';

export const metadata = sectionMetadata('courts');

export default function Page() {
  return <SectionPage id="courts" />;
}
