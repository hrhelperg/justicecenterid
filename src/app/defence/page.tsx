import { SectionPage, sectionMetadata } from '@/components/pages/SectionPage';

export const metadata = sectionMetadata('defence');

export default function Page() {
  return <SectionPage id="defence" />;
}
