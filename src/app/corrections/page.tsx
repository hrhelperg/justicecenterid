import { SectionPage, sectionMetadata } from '@/components/pages/SectionPage';

export const metadata = sectionMetadata('corrections');

export default function Page() {
  return <SectionPage id="corrections" />;
}
