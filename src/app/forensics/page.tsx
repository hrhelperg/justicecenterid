import { SectionPage, sectionMetadata } from '@/components/pages/SectionPage';

export const metadata = sectionMetadata('forensics');

export default function Page() {
  return <SectionPage id="forensics" />;
}
