import { SectionPage, sectionMetadata } from '@/components/pages/SectionPage';

export const metadata = sectionMetadata('public-safety');

export default function Page() {
  return <SectionPage id="public-safety" />;
}
